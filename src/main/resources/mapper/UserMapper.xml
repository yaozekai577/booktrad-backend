<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.booktrad.user.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.booktrad.user.entity.User">
        SELECT * FROM user WHERE username = #{username} AND deleted_at IS NULL
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAllUsers" resultType="com.booktrad.user.entity.User">
        SELECT * FROM user
    </select>

    <!-- 动态条件查询用户 -->
    <select id="selectUsersByCondition" resultType="com.booktrad.user.entity.User">
        SELECT * FROM user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            AND deleted_at IS NULL
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID查询用户（用于订单创建，忽略逻辑删除） -->
    <select id="selectUserForOrder" resultType="com.booktrad.user.entity.User">
        SELECT
            id,
            username,
            password,
            email,
            phone,
            role,
            status,
            created_at,
            updated_at,
            deleted_at,
            extra_info
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 更新用户密码 -->
    <update id="updatePassword">
        UPDATE user
        SET password = #{newPassword},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

    <!-- 更新用户作为卖家的评分统计 -->
    <update id="updateSellerRating">
        UPDATE user
        SET seller_rating_score = #{newScore},
            seller_rating_count = #{newCount},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

    <!-- 更新用户作为买家的评分统计 -->
    <update id="updateBuyerRating">
        UPDATE user
        SET buyer_rating_score = #{newScore},
            buyer_rating_count = #{newCount},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

    <!-- 根据用户ID查询用户（用于评价系统） -->
    <select id="selectUserById" resultType="com.booktrad.user.entity.User">
        SELECT
            id,
            username,
            password,
            seller_rating_score,
            seller_rating_count,
            buyer_rating_score,
            buyer_rating_count,
            email,
            phone,
            role,
            status,
            created_at,
            updated_at,
            deleted_at,
            extra_info
        FROM user
        WHERE id = #{userId}
    </select>

    <!-- 统计卖家收到的评价数量 -->
    <select id="countSellerReviews" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM order_review
        WHERE reviewee_id = #{sellerId}
        AND reviewer_role = 1 <!-- 1表示买家评价卖家 -->
    </select>

    <!-- 查询卖家在售的书籍列表 -->
    <select id="selectSellerOnSaleBooks" resultType="java.util.Map">
        SELECT
            b.id,
            b.title,
            b.author,
            b.publisher,
            b.isbn,
            b.price,
            b.book_condition,
            b.cover_image,
            b.status,
            b.view_count,
            b.created_at
        FROM book b
        WHERE b.seller_id = #{sellerId}
          AND b.status = 1
          AND b.is_banned = 0
          AND b.deleted_at IS NULL
        ORDER BY b.created_at DESC
    </select>

    <!-- 查询卖家收到的评价列表 -->
    <select id="selectSellerReviews" resultType="java.util.Map">
        SELECT
            r.id,
            r.order_id,
            r.reviewer_id,
            reviewer.username AS reviewer_username,
            r.reviewee_id,
            reviewee.username AS reviewee_username,
            r.reviewer_role,
            r.rating,
            r.content,
            r.is_anonymous,
            r.reply_content,
            r.reply_time,
            r.created_at
        FROM order_review r
        LEFT JOIN user reviewer ON r.reviewer_id = reviewer.id
        LEFT JOIN user reviewee ON r.reviewee_id = reviewee.id
        WHERE r.reviewee_id = #{sellerId}
        ORDER BY r.created_at DESC
    </select>
    <!-- 管理员分页查询用户 -->
    <select id="selectUserPage" resultType="com.booktrad.user.entity.User">
        SELECT
            id,
            username,
            email,
            phone,
            role,
            status,
            ban_reason,
            seller_rating_score,
            seller_rating_count,
            buyer_rating_score,
            buyer_rating_count,
            created_at,
            updated_at,
            extra_info
        FROM user
        WHERE deleted_at IS NULL
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID查询用户实体（手写SQL） -->
    <select id="selectUserEntityById" resultType="com.booktrad.user.entity.User">
        SELECT
            id,
            username,
            email,
            phone,
            role,
            status,
            ban_reason,
            seller_rating_score,
            seller_rating_count,
            buyer_rating_score,
            buyer_rating_count,
            created_at,
            updated_at,
            extra_info
        FROM user
        WHERE id = #{id}
        AND deleted_at IS NULL
    </select>

    <!-- 更新用户封禁状态 -->
    <update id="updateUserBanStatus">
        UPDATE user
        SET status = #{status},
            ban_reason = #{banReason},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

    <!-- 更新用户信息（邮箱和电话） -->
    <update id="updateUserInfo">
        UPDATE user
        SET email = #{email},
            phone = #{phone},
            updated_at = NOW()
        WHERE id = #{userId}
          AND deleted_at IS NULL
    </update>

    <!-- 统计每日新增用户 -->
    <select id="selectDailyNewUsers" resultType="java.util.Map">
        SELECT
            DATE_FORMAT(created_at, '%Y-%m-%d') AS date,
            COUNT(*) AS count
        FROM user
        WHERE created_at >= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
        AND deleted_at IS NULL
        GROUP BY date
        ORDER BY date
    </select>

    <!-- 统计总用户数 -->
    <select id="countTotalUsers" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user
        WHERE deleted_at IS NULL
    </select>

</mapper>